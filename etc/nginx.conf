
server {
    listen 80;

    location ~ /uploads/.* {
    	root /home/box/web/uploads;
    }

    location ~ .*\.(.+)$ {
    	root /home/box/web/public;
    }

    ﻿location ~ .*\/(\w+)$ {
    	return 404;
    }
}
